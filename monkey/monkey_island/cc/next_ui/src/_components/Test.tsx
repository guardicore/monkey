'use client';
// import useAuth from '@/hooks/useAuth';
import LogoutButton from '@/_components/logoutButton/LogoutButton';
// import LoginPage from '@/_components/login/Login';

const Test = () => {
    // const isAuthenticated: boolean = useAuth();

    // if (isAuthenticated) {
    return <LogoutButton />;
    // } else {
    // return <LoginPage />;
    // }
};

export default Test;
