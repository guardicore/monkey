{
    "properties": {
        "exclude_username_prefix": {
            "title": "Excluded username prefix",
            "description": "Mimikatz will not collect credentials for any user whose username starts with this prefix.",
            "type": "string",
            "default": ""
        }
    }
}
